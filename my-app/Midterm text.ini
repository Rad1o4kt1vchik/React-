Mini-проект: “MiniLibrary” Маршруты /books — список книг

/add-book — форма добавления книги Функционал 1) Страница /books Список хранится в useState.

При первом рендере загрузить из localStorage (через useEffect). (бонус)

Поле поиска по названию.

Фильтр по жанру (select: “all / fiction / nonfiction / tech”).

Каждая карточка — компонент , получает данные через props и обёрнут в React.memo.

Кнопка Delete на карточке. Функция удаления объявлена в родительском компоненте и прокинута в карточку; обернуть в useCallback. 2) Страница /add-book Форма на Formik + Yup с полями:

title — обязательно, минимум 2 символа

author — обязательно

genre — обязательно, один из: fiction | nonfiction | tech

rating — число от 0 до 5 (можно дробное, например 4.5)

При сабмите:

Добавить книгу в список (id с Date.now()).

Перейти обратно на /books.

3) Навигация (Router) Простое меню/хедер с двумя ссылками: Books и Add Book.

Маршруты: /books, /add-book, * → 404.

Требования к компонентам BookCard:

показывает title, author, genre, rating


кнопка Delete

обёрнут в React.memo

Родительский список:

хранит массив книг в стейте

передаёт onDelete в карточку (обёрнутый в useCallback)

фильтрация .filter() по названию (регистронезависимо) Структура проекта src/ ├─ components/ │ └─ BookCard.jsx ├─ pages/ │ ├─ Books.jsx │ └─ AddSBook.jsx ├─ App.jsx ├─ main.jsx └─ index.css